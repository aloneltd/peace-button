// Legacy constants — no longer used in new flow
export const CATEGORIES: never[] = [];
export const BASE_NEEDS: never[] = [];
export const CATEGORY_SPECIFIC_NEEDS: Record<string, never[]> = {};
export const RISK_FACTORS: never[] = [];
export const CRISIS_RESOURCES = { text: '', numbers: [] };
export const REPAIR_LIBRARY: Record<string, Record<string, string>> = {};
